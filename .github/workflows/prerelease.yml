name: Pre release pipelien

on:
  release:
    types:
      - prereleased

env:
  GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
  TAG: ${{ github.event.release.tag_name }}
  #TAG: v3.0.0
  PFX_CERTIFICATE_BASE64: ${{ secrets.PFX_CERTIFICATE_BASE64 }} # base64 encoded
  GH_TOKEN: ${{ secrets.GH_TOKEN }}
  GPG_MAIL: info@newrelic.com
  GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
  GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
jobs:
#
#  validate:
#    name: Validate code via linters
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v2
#      - name: Validate code
#        run: make ci/validate
#
#  snyk:
#    name: Run security checks via snyk
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v2
#      - name: Run Snyk to check for vulnerabilities
#        uses: snyk/actions/golang@master
#        env:
#          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
#        with:
#          args: --severity-threshold=high
#
#  test-linux:
#    name: Run unit tests on linux
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v2
#      - name: Unit tests
#        run: make ci/test
#
#  # can't run this step inside of container because of tests specific
#  test-integration-linux:
#    name: Run integration tests on linux
#    runs-on: ubuntu-latest
#    defaults:
#      run:
#        working-directory: src/github.com/${{ github.event.repository.full_name }}
#    steps:
#      - name: Check out code
#        uses: actions/checkout@v2
#        with:
#          fetch-depth: 1
#          path: src/github.com/${{ github.event.repository.full_name }}
#      - name: Install Go
#        uses: actions/setup-go@v2
#        with:
#          go-version: '1.9.7'
#      - name: Integration test
#        env:
#          GOPATH: ${{ github.workspace }}
#        run: make integration-test
#
  prerelease-nix-win:
    name: Build binaries for nix & win and publish on GH Releases
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Pre release
        run: make ci/prerelease
      - name: Upload exe binaries to GHA Cache
        uses: actions/upload-artifact@v2
        with:
          name: win-binaries
          path: dist/nri-redis-win_windows_*/*.exe

  prerelease-win:
    runs-on: windows-2019
    needs: [prerelease-nix-win]
    env:
      GOPATH: ${{ github.workspace }}
    defaults:
      run:
        working-directory: src/github.com/newrelic/nri-redis
    strategy:
      matrix:
        goarch: [amd64,386]
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          path: src/github.com/newrelic/nri-redis
      - name: Get PFX certificate from GH secrets
        shell: bash
        run: printf "%s" "$PFX_CERTIFICATE_BASE64" | base64 -d - > pkg/windows/nri-${{ matrix.goarch }}-installer/mycert.pfx
      - name: Download binaries from GH Cache
        uses: actions/download-artifact@v2
        with:
          name: win-binaries
          path: target/
      - name: Place exe's in correct place so that Wix can take them
        shell: bash
        run: |
          pwd
          ls
          cp target/win-binaries/nri-redis-win_windows_${{ matrix.goarch }}/*.exe  target/bin/windows_${{ matrix.goarch }}/
      - name: Create MSI
        shell: pwsh
        run: .\win_build.ps1 -arch ${{ matrix.goarch }} -tag "$env:TAG"
      - name: Create and upload win zip
        shell: bash
        run: build/create_upload_win_zip.sh redis ${{ matrix.goarch }} ${TAG} ${GITHUB_TOKEN}
      - name: Push msi to GH Release assets
        shell: bash
        run:  hub release edit -a "pkg/windows/nri-${{ matrix.goarch }}-installer/bin/Release/nri-redis-${{ matrix.goarch }}.${TAG:1}.msi" -m ${TAG} ${TAG}


